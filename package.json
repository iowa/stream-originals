{
  "name": "stream-originals",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "upgrade-dependencies": "ncu -u"
  },
  "devDependencies": {
    "npm-check-updates": "^19.1.2",
    "prettier": "^3.6.2",
    "turbo": "^2.6.0",
    "typescript": "5.9.3",
    "vitest": "^4.0.6"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=18"
  }
}
